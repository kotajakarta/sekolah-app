# VITE_API_BASE_URL: Base URL of the backend-api gateway.
# Use full URL in production (e.g., https://api.domainku.com/api/v1).
# In local development, you can leave this empty or use /api/v1
# because Vite Proxy will forward /api/v1 to http://localhost:8080/api/v1.
VITE_API_BASE_URL=""
